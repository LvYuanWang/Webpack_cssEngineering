:root {
  --center: {
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
  };
  --contentCenter: {
    display: flex;
    justify-content: center;
    align-items: center;
  }
}

.item {
  @apply --center;
  @apply --contentCenter;
}